= form_with url: search_path, method: :get do |f|
  p
    = f.text_field :query, placeholder: 'Search...', class: 'form-control me-2'
  p
    = f.label :resource, 'For:'
    = f.select :resource, ['questions', 'answers', 'comments', 'users', 'all'], class: 'form-select'
  p
    = f.submit 'Search', class: 'btn btn-outline-primary btn-sm'
