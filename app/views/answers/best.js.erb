$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% if !@answer.errors.present? && @question.best_answer %>
  $('.answers .best').html('<%= j render 'answers/best_answer', resource: @question.best_answer %>');
  $('.answers table tbody').html('<%= j render @question.other_answers %>');
<% end %>
