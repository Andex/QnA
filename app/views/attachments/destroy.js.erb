<% if !@file.persisted? %>
  $('.flash-messages').html('<%= render 'shared/flash_messages' %>');
  $('.<%= @file.record_type.downcase %>-<%= @file.record_id %>-attachments .files #<%= @file.id %>').remove()
<% end %>